# V24-A Release Candidate

این Build یک Feature Release نیست؛ Stabilization Release است.

تمرکز:
1. رفع ریشه‌ای خرابی Script V23.2.
2. جلوگیری از قفل‌شدن موبایل پس از Sidebar/Home.
3. تکمیل CRUD حساب‌ها.
4. جداکردن Demo data از حساب‌های جدید.
5. آماده‌سازی Baseline برای V24-B (OCR/Accounts validation) و سپس Release Candidate نهایی.

وضعیت Backend: در V24-A اختیاری و غیرفعال تا زمان درج `supabaseUrl` و `supabaseAnonKey` در `config.js`.
