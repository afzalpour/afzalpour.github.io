# گزارش تست حساب‌یار V21

## تست Syntax
- `demo.html`: تمام اسکریپت‌های inline با `node --check` بررسی شدند؛ خطای Syntax: صفر.
- `index.html`: تمام اسکریپت‌های inline با `node --check` بررسی شدند؛ خطای Syntax: صفر.
- `sw.js`: Cache نسخه V21 و ساختار Service Worker بررسی شد.

## تست OCR موبایل
Viewport تست: `390×844`.

سناریو:
1. ورود به صفحه اسکن هوشمند.
2. انتخاب فایل از ورودی Upload.
3. بررسی نمایش فوری تصویر.
4. بررسی اینکه `src` پیش‌نمایش از نوع `data:image/...` است.
5. اجرای مسیر OCR با Worker شبیه‌سازی‌شده تا ارتباط Button → همان تصویر → نتیجه → فرم قطعی بررسی شود.

نتیجه:
- پیش‌نمایش تصویر: موفق.
- منبع تصویر در رابط: «آپلود / گالری» نمایش داده شد.
- اجرای دکمه OCR روی تصویر پیش‌نمایش‌شده: موفق.
- متن نمونه در نتیجه OCR قرار گرفت.
- مبلغ نمونه در فرم استخراج شد.
- Page/Runtime Error: صفر.

> به علت محدودیت شبکه محیط تست، دانلود واقعی فایل‌های WASM و مدل `fas/eng` از CDN داخل Chromium این محیط اجرا نشد. مسیر واقعی V21 از Tesseract.js و چند CDN fallback استفاده می‌کند و خطای بارگذاری را به‌صورت واضح نشان می‌دهد.

## تست Voice fallback
- خطای `service-not-allowed` به‌صورت مستقیم شبیه‌سازی و پیام اختصاصی آن بررسی شد.
- نبود SpeechRecognition/محیط نامناسب باعث باز شدن مسیر جایگزین می‌شود، نه بن‌بست.
- فرمان متنی «گزارش‌ها را باز کن» با همان Intent Engine اجرا و صفحه گزارش باز شد.
- Runtime Error: صفر.

## تست PWA/Mobile
- Bottom Navigation: اندازه متن محاسبه‌شده `13.5px` در حالت استاندارد.
- منوی جانبی در موبایل کاملاً خارج از Viewport مخفی می‌شود و فقط در حالت Open ظاهر می‌شود.
- Cache key به `hesabyar-v21-shell-1` تغییر کرده تا نسخه قدیمی PWA پس از Update باقی نماند.
