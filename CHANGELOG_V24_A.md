# حساب‌یار V24-A — Delivery Candidate / Mobile & Core Stabilization

## هدف
این نسخه قابلیت جدید اضافه نمی‌کند. هدف، تبدیل V23.2 به یک Baseline قابل تست برای تحویل سه‌روزه است.

## اصلاحات بحرانی
- کد V23.2 مربوط به Camera/Gallery/File/OCR که اشتباهاً داخل Template چاپ فاکتور قرار گرفته بود، از آن خارج و به Script واقعی برنامه منتقل شد.
- ساختار JavaScript همه Scriptهای inline با `node --check` بررسی شد.
- Mobile Navigation به یک Controller نهایی V24 متصل شد؛ باز/بسته شدن Sidebar، Backdrop و Scroll Lock یک State واحد دارند.
- پس از انتخاب «خانه» یا هر دستور Sidebar، منوی موبایل و Backdrop به‌صورت قطعی بسته می‌شوند.
- Mobile Shell از مخفی‌شدن `main/content/active page` محافظت می‌کند.

## حساب‌ها
- ایجاد حساب حفظ شد.
- ویرایش حساب اضافه شد (تکمیل قابلیت موجود).
- حذف امن حساب اضافه شد: حساب بدون وابستگی قابل حذف است؛ حساب دارای گردش/وابستگی برای حفظ صحت داده حذف نمی‌شود.
- مانده اولیه قابل ویرایش است و مانده محاسباتی طبق تراکنش‌ها ادامه دارد.

## داده واقعی / Demo
- حساب Demo همچنان داده نمونه دارد.
- حساب محلی/ابری جدید دیگر با حساب‌ها، تراکنش‌ها و بودجه‌های Demo پر نمی‌شود.
- fallback ساختگی نمودار ریتم هزینه حذف شد؛ در نبود داده، مقادیر ساختگی تولید نمی‌شود.

## PWA
- فایل‌های `manifest.webmanifest`, `sw.js`, `offline-db.js`, `config.js`, `app-icon.svg` در بسته حضور دارند.
- Cache به `hesabyar-v24-a-cache` ارتقا یافت.

## نکته OCR
- مسیر UI و اجرای Handler OCR اکنون واقعاً در Main App اجرا می‌شود.
- موتور OCR واقعی همچنان Browser-side Tesseract است و اولین بارگذاری به CDN/اینترنت وابسته است؛ Backend OCR در V24-A اضافه نشده است.
