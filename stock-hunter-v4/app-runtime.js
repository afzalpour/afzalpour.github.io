function headers(){const k=cfg.SUPABASE_PUBLISHABLE_KEY||cfg.publishableKey||'';return{apikey:k,Accept:'application/json'}}
function marketSessionTehran(){const parts=new Intl.DateTimeFormat('en-US',{timeZone:'Asia/Tehran',weekday:'short',hour:'2-digit',minute:'2-digit',hour12:false}).formatToParts(new Date()),get=t=>parts.find(p=>p.type===t)?.value||'',wd=get('weekday'),hm=Number(get('hour'))*60+Number(get('minute'));if(!['Sat','Sun','Mon','Tue','Wed'].includes(wd)||hm<8*60+25||hm>17*60)return{open:false,label:'بازار بورس در این لحظه تعطیل است',detail:'ساعات فعالیت بازار سرمایه در روزهای معاملاتی از ۸:۲۵ تا ۱۷:۰۰ است.'};const active=[];if(hm>=8*60+25&&hm<=15*60)active.push('صندوق‌های درآمد ثابت');if(hm>=8*60+45&&hm<9*60)active.push('پیش‌گشایش سهام');if(hm>=9*60&&hm<=12*60+30)active.push('سهام، صندوق‌های سهامی و حق‌تقدم');if(hm>=9*60&&hm<=15*60)active.push('املاک، تسهیلات مسکن و بدهی');if(hm>=10*60+45&&hm<=15*60)active.push('سپرده کالایی انرژی');if(hm>=11*60+45&&hm<=17*60)active.push('طلا، نقره، زعفران و گواهی کالایی');return{open:true,label:'اطلاعات لحظه‌ای بازار دریافت شد',detail:active.length?`بازارهای فعال: ${active.join('، ')}`:'بخشی از بازار در مرحله پیش‌گشایش است'}}
async function load(){const base=String(cfg.SUPABASE_URL||cfg.supabaseUrl||'').replace(/\/$/,''),key=cfg.SUPABASE_PUBLISHABLE_KEY||cfg.publishableKey||'';if(!base||!key){showFeed('bad','اتصال اطلاعات بازار تنظیم نشده','آدرس پایگاه داده در دسترس نیست');return}try{const [sr,hr]=await Promise.all([fetch(`${base}/rest/v1/${cfg.TABLE||'stock_hunter_signals_v4'}?select=*&order=fast_score.desc&limit=2000`,{headers:headers(),cache:'no-store'}),fetch(`${base}/rest/v1/stock_hunter_feed_health_v4?select=*&id=eq.local-agent&limit=1`,{headers:headers(),cache:'no-store'})]);if(!sr.ok)throw new Error('خطا در دریافت اطلاعات');rows=(await sr.json()).map(norm).sort((a,b)=>b.fast-a.fast);rows.forEach(x=>x.analyzed=true);let health=[];if(hr.ok)health=await hr.json();const h=health[0],age=h?.last_feed_at?Date.now()-Date.parse(h.last_feed_at):Infinity,session=marketSessionTehran();if(!session.open){showFeed('closed',session.label,h?.last_feed_at?`آخرین اطلاعات ثبت‌شده: ${new Date(h.last_feed_at).toLocaleTimeString('fa-IR',{timeZone:'Asia/Tehran'})}`:session.detail)}else if(h&&h.status==='ok'&&age<60000){const when=new Date(h.last_feed_at).toLocaleTimeString('fa-IR',{timeZone:'Asia/Tehran'}),count=Number(h.symbols||0).toLocaleString('fa-IR');showFeed('ok',session.label,`${session.detail} — آخرین دریافت: ${when} — ${count} نماد در آخرین ارسال`)}else if(h&&age<180000){showFeed('warn','اطلاعات بازار با تأخیر دریافت می‌شود',`آخرین ارتباط ${new Date(h.last_feed_at).toLocaleTimeString('fa-IR',{timeZone:'Asia/Tehran'})}`)}else{showFeed('bad','اطلاعات لحظه‌ای معاملات در دسترس نیست','در ساعات رسمی بازار داده تازه دریافت نشده است؛ احتمال تعطیلی رسمی بازار یا قطع مسیر دریافت داده وجود دارد.')}render();const urgent=rows.filter(x=>x.hunt==='شکار ویژه'||x.hunt==='هشدار فوری');if(sound&&session.open&&urgent.some(x=>!seen.has(x.id))){try{const c=new AudioContext(),o=c.createOscillator(),g=c.createGain();o.connect(g);g.connect(c.destination);g.gain.value=.035;o.frequency.value=900;o.start();o.stop(c.currentTime+.12)}catch{}}urgent.forEach(x=>seen.add(x.id))}catch(e){showFeed('bad','خطا در دریافت اطلاعات بازار',e.message)}}
function showFeed(type,title,sub){$('feedBadge').className=`feed-badge ${type}`;$('feedBadge').textContent=title;$('feedState').textContent=title;$('scanTimes').textContent=sub;const quiet=type==='ok'||type==='closed';$('alertBox').hidden=quiet;if(!quiet)$('alertBox').textContent=sub}
function openColumns(){$('columnPanel').classList.add('open');$('columnPanel').setAttribute('aria-hidden','false');$('panelBackdrop').hidden=false}function closeColumns(){$('columnPanel').classList.remove('open');$('columnPanel').setAttribute('aria-hidden','true');$('panelBackdrop').hidden=true}function renderColumnOptions(){$('columnOptions').innerHTML=columns.map(c=>`<label><input type="checkbox" data-col="${c[0]}" ${visible.has(c[0])?'checked':''}>${c[1]}</label>`).join('');$('columnOptions').querySelectorAll('input').forEach(i=>i.onchange=()=>{i.checked?visible.add(i.dataset.col):visible.delete(i.dataset.col);if(visible.size<4){visible.add(i.dataset.col);i.checked=true}localStorage.setItem('sh_columns_v407',JSON.stringify([...visible]));render()})}
document.addEventListener('click',e=>{const b=e.target.closest('[data-id]');if(b)openDetail(b.dataset.id)});$('closeDetail').onclick=()=>$('detailDialog').close();$('printDetailBtn').onclick=printDetail;$('columnsBtn').onclick=openColumns;$('closeColumns').onclick=closeColumns;$('panelBackdrop').onclick=closeColumns;$('defaultColumns').onclick=()=>{visible=new Set(defaultKeys);localStorage.setItem('sh_columns_v407',JSON.stringify([...visible]));renderColumnOptions();render()};$('allColumns').onclick=()=>{visible=new Set(columns.map(c=>c[0]));localStorage.setItem('sh_columns_v407',JSON.stringify([...visible]));renderColumnOptions();render()};$('prevPage').onclick=()=>{if(page>1){page--;render()}};$('nextPage').onclick=()=>{page++;render()};for(const id of ['search','hunt','decision','pageSize'])$(id).addEventListener(id==='search'?'input':'change',()=>{page=1;render()});$('refreshBtn').onclick=load;$('soundBtn').onclick=()=>{sound=!sound;$('soundBtn').textContent=sound?'🔔 هشدار روشن':'🔕 هشدار خاموش'};renderColumnOptions();load();setInterval(load,Number(cfg.REFRESH_MS||15000));if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js?v=4.0.9').catch(()=>{});