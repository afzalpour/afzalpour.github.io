(()=>{'use strict';
const cfg=window.STOCK_HUNTER_CONFIG||{}, $=id=>document.getElementById(id), n=v=>{const x=Number(v);return Number.isFinite(x)?x:null}, fa=(v,d=2)=>n(v)==null?'—':Number(v).toLocaleString('fa-IR',{maximumFractionDigits:d}), esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
let rows=[],page=1,sound=true,seen=new Set();
const columns=[['symbol','نماد',true,14],['hunt','شکار',true,12],['decision','تصمیم',true,11],['fast','امتیاز لحظه‌ای',true,10],['accel','شتاب سیگنال',false,9],['p2','تداوم ۲ روزه',true,10],['p3','تداوم ۳ روزه',false,10],['risk','ریسک',true,8],['qi','عدم‌تعادل صف',false,9],['ofi','جریان سفارش',false,9],['bidStack','انباشت خرید',false,9],['askPull','عقب‌نشینی فروش',false,9],['rvol','حجم نسبی',false,9],['price','قیمت',true,10],['details','جزئیات',true,7]];
const defaultKeys=columns.filter(c=>c[2]).map(c=>c[0]);let visible=new Set(JSON.parse(localStorage.getItem('sh_columns')||'null')||defaultKeys);
function norm(r){return{id:String(r.id),symbol:r.symbol||'—',company:r.company_name||'—',hunt:r.hunt_state||'عادی',decision:r.decision||'نخر',fast:+r.fast_score||0,fastProb:+r.fast_probability||0,accel:+r.signal_accel||0,cont:+r.continuation_score||0,p2:+r.prob_2d||0,p3:+r.prob_3d||0,risk:+r.risk_score||0,qi:+r.qi||0,ofi:+r.ofi||0,bidStack:+r.bid_stack_15s||0,askPull:+r.ask_pull_15s||0,rvol:+r.daily_rvol||0,last:+r.last_price||0,close:+r.closing_price||0,yesterday:+r.yesterday_price||0,rsi:+r.rsi_5m||0,ema9:+r.ema9_5m||0,ema21:+r.ema21_5m||0,vwap:+r.vwap||0,atr:+r.atr_5m||0,technical:+r.technical_score||0,micro:+r.microprice||0,abs:+r.absorption||0,cancel:+r.cancellation_ratio||0,pv:+r.price_velocity||0,tradeAccel:+r.trade_accel||0,entry:+r.entry_price||0,entryLow:+r.entry_low||0,entryHigh:+r.entry_high||0,stop:+r.stop_loss||0,t1:+r.target_1||0,t2:+r.target_2||0,t3:+r.target_3||0,rr:+r.risk_reward||0,reason:r.reason||'',updated:r.updated_at||''}}
function hc(v){return v==='شکار ویژه'?'special':v==='هشدار فوری'?'urgent':v==='شکار زودهنگام'?'early':v==='مستعد تداوم'?'continue':'watch'}function dc(v){return v.includes('خرید')?'buy':v==='ورود اولیه'?'early':v==='تحت نظر'?'watch':'no'}function rc(v){return v<40?'risk-low':v<65?'risk-mid':'risk-high'}
function cell(k,x){switch(k){case'symbol':return `<div class="symbol">${esc(x.symbol)}</div><div class="company">${esc(x.company)}</div>`;case'hunt':return `<span class="badge ${hc(x.hunt)}">${esc(x.hunt)}</span>`;case'decision':return `<span class="badge ${dc(x.decision)}">${esc(x.decision)}</span>`;case'fast':return `<b>${fa(x.fast,1)}</b><div class="company">احتمال ${fa(x.fastProb,1)}٪</div>`;case'accel':return fa(x.accel,1);case'p2':return `${fa(x.p2,1)}٪`;case'p3':return `${fa(x.p3,1)}٪`;case'risk':return `<span class="${rc(x.risk)}">${fa(x.risk,1)}</span>`;case'qi':return fa(x.qi,2);case'ofi':return fa(x.ofi,2);case'bidStack':return `${fa(x.bidStack,1)}٪`;case'askPull':return `${fa(x.askPull,1)}٪`;case'rvol':return x.rvol?`${fa(x.rvol,2)} برابر`:'—';case'price':return fa(x.last,0);case'details':return `<button class="detail-btn" data-id="${esc(x.id)}">نمایش</button>`;default:return'—'}}
function filtered(){const q=$('search').value.trim(),h=$('hunt').value,d=$('decision').value;return rows.filter(x=>(!q||x.symbol.includes(q)||x.company.includes(q))&&(!h||x.hunt===h)&&(!d||x.decision===d))}
function render(){const a=filtered(),size=+$('pageSize').value||25,pages=Math.max(1,Math.ceil(a.length/size));if(page>pages)page=pages;const slice=a.slice((page-1)*size,page*size),cols=columns.filter(c=>visible.has(c[0]));$('thead').innerHTML='<tr><th style="width:42px">#</th>'+cols.map(c=>`<th style="width:${c[3]}%">${c[1]}</th>`).join('')+'</tr>';$('tbody').innerHTML=slice.length?slice.map((x,i)=>'<tr><td>'+(((page-1)*size+i+1).toLocaleString('fa-IR'))+'</td>'+cols.map(c=>`<td>${cell(c[0],x)}</td>`).join('')+'</tr>').join(''):`<tr><td colspan="${cols.length+1}">داده‌ای مطابق فیلتر وجود ندارد.</td></tr>`;$('pageInfo').textContent=`صفحه ${page.toLocaleString('fa-IR')} از ${pages.toLocaleString('fa-IR')} — ${a.length.toLocaleString('fa-IR')} نماد`;$('prevPage').disabled=page<=1;$('nextPage').disabled=page>=pages;renderMobile(slice);updateSummary();}
function renderMobile(a){$('mobileList').innerHTML=a.length?a.map(x=>`<article class="mobile-card"><div class="mobile-head"><div><div class="mobile-symbol">${esc(x.symbol)}</div><div class="company">${esc(x.company)}</div></div><div><span class="badge ${hc(x.hunt)}">${esc(x.hunt)}</span> <span class="badge ${dc(x.decision)}">${esc(x.decision)}</span></div></div><div class="mobile-metrics"><div><span>امتیاز</span><b>${fa(x.fast,1)}</b></div><div><span>تداوم ۲ روزه</span><b>${fa(x.p2,1)}٪</b></div><div><span>ریسک</span><b class="${rc(x.risk)}">${fa(x.risk,1)}</b></div></div><div class="mobile-foot"><b>${fa(x.last,0)}</b><button class="detail-btn" data-id="${esc(x.id)}">جزئیات</button></div></article>`).join(''):'<div class="alert-box">داده‌ای مطابق فیلتر وجود ندارد.</div>'}
function updateSummary(){$('specialCount').textContent=rows.filter(x=>x.hunt==='شکار ویژه').length.toLocaleString('fa-IR');$('urgentCount').textContent=rows.filter(x=>x.hunt==='هشدار فوری').length.toLocaleString('fa-IR');$('buyCount').textContent=rows.filter(x=>x.decision==='خرید قوی'||x.decision==='ورود اولیه').length.toLocaleString('fa-IR');const t=rows[0];$('topSymbol').textContent=t?.symbol||'—';$('topMeta').textContent=t?`${t.company} — امتیاز ${fa(t.fast,1)}`:'—'}
function metric(l,v,c=''){return `<div class="metric"><span>${l}</span><b class="${c}">${v}</b></div>`}function openDetail(id){const x=rows.find(r=>r.id===id);if(!x)return;$('dSymbol').textContent=x.symbol;$('dCompany').textContent=x.company;$('detailBody').innerHTML=`<div class="detail-grid"><div class="section-title">تصمیم و وضعیت</div>${metric('وضعیت شکار',esc(x.hunt))}${metric('تصمیم',esc(x.decision))}${metric('امتیاز لحظه‌ای',fa(x.fast,1))}${metric('ریسک',fa(x.risk,1),rc(x.risk))}<div class="section-title">رفتار سفارش‌ها</div>${metric('شتاب سیگنال',fa(x.accel,1))}${metric('عدم‌تعادل صف',fa(x.qi,2))}${metric('عدم‌تعادل جریان سفارش',fa(x.ofi,2))}${metric('انباشت خرید ۱۵ ثانیه',fa(x.bidStack,1)+'٪')}${metric('عقب‌نشینی فروش ۱۵ ثانیه',fa(x.askPull,1)+'٪')}${metric('جذب عرضه',fa(x.abs,1)+'٪')}${metric('لغو سفارش',fa(x.cancel,1)+'٪')}${metric('سرعت قیمت',fa(x.pv,2))}<div class="section-title">تکنیکال و تداوم</div>${metric('احتمال تداوم ۲ روزه',fa(x.p2,1)+'٪')}${metric('احتمال تداوم ۳ روزه',fa(x.p3,1)+'٪')}${metric('شاخص قدرت نسبی',fa(x.rsi,1))}${metric('میانگین نمایی ۹ دوره',fa(x.ema9,0))}${metric('میانگین نمایی ۲۱ دوره',fa(x.ema21,0))}${metric('میانگین موزون قیمت',fa(x.vwap,0))}${metric('دامنه نوسان واقعی',fa(x.atr,0))}${metric('حجم نسبی روزانه',x.rvol?fa(x.rvol,2)+' برابر':'—')}<div class="section-title">برنامه معامله</div>${metric('ورود پیشنهادی',fa(x.entry,0))}${metric('بازه ورود',`${fa(x.entryLow,0)} تا ${fa(x.entryHigh,0)}`)}${metric('حد ضرر',fa(x.stop,0),'risk-high')}${metric('هدف اول',fa(x.t1,0),'risk-low')}${metric('هدف دوم',fa(x.t2,0),'risk-low')}${metric('هدف سوم',fa(x.t3,0),'risk-low')}${metric('نسبت سود به زیان',fa(x.rr,2))}<div class="detail-text"><b>منطق انتخاب:</b> ${esc(x.reason||'ترکیب رفتار سفارش‌ها، قدرت تقاضا، تداوم حرکت و کنترل ریسک')}</div></div>`;$('detailDialog').showModal()}
function headers(){const k=cfg.SUPABASE_PUBLISHABLE_KEY||cfg.publishableKey||'';return{apikey:k,Accept:'application/json'}}
async function load(){const base=String(cfg.SUPABASE_URL||cfg.supabaseUrl||'').replace(/\/$/,''),key=cfg.SUPABASE_PUBLISHABLE_KEY||cfg.publishableKey||'';if(!base||!key){showFeed('bad','اتصال تنظیم نشده','آدرس پایگاه داده در دسترس نیست');return}try{const [sr,hr]=await Promise.all([fetch(`${base}/rest/v1/${cfg.TABLE||'stock_hunter_signals_v4'}?select=*&order=fast_score.desc&limit=600`,{headers:headers(),cache:'no-store'}),fetch(`${base}/rest/v1/stock_hunter_feed_health_v4?select=*&id=eq.local-agent&limit=1`,{headers:headers(),cache:'no-store'})]);if(!sr.ok)throw new Error('خطا در دریافت اطلاعات');rows=(await sr.json()).map(norm);let health=[];if(hr.ok)health=await hr.json();const h=health[0],age=h?.last_feed_at?Date.now()-Date.parse(h.last_feed_at):Infinity;if(h&&h.status==='ok'&&age<60000){showFeed('ok','خوراک بازار زنده است',`${Number(h.symbols||0).toLocaleString('fa-IR')} نماد — آخرین ارسال ${new Date(h.last_feed_at).toLocaleTimeString('fa-IR')}`)}else if(h&&age<180000){showFeed('warn','خوراک با تأخیر است',`آخرین ارتباط ${new Date(h.last_feed_at).toLocaleTimeString('fa-IR')}`)}else{showFeed('bad','پل داده فعال نیست','برای داده زنده، پل داده ویندوز باید روی رایانه روشن و در حال اجرا باشد.')}render();const urgent=rows.filter(x=>x.hunt==='شکار ویژه'||x.hunt==='هشدار فوری');if(sound&&urgent.some(x=>!seen.has(x.id))){try{const c=new AudioContext(),o=c.createOscillator(),g=c.createGain();o.connect(g);g.connect(c.destination);g.gain.value=.035;o.frequency.value=900;o.start();o.stop(c.currentTime+.12)}catch{}}urgent.forEach(x=>seen.add(x.id))}catch(e){showFeed('bad','خطا در دریافت داده',e.message)}}
function showFeed(type,title,sub){$('feedBadge').className=`feed-badge ${type}`;$('feedBadge').textContent=title;$('feedState').textContent=title;$('scanTimes').textContent=sub;$('alertBox').hidden=type==='ok';if(type!=='ok')$('alertBox').textContent=sub}
function openColumns(){$('columnPanel').classList.add('open');$('columnPanel').setAttribute('aria-hidden','false');$('panelBackdrop').hidden=false}function closeColumns(){$('columnPanel').classList.remove('open');$('columnPanel').setAttribute('aria-hidden','true');$('panelBackdrop').hidden=true}function renderColumnOptions(){$('columnOptions').innerHTML=columns.map(c=>`<label><input type="checkbox" data-col="${c[0]}" ${visible.has(c[0])?'checked':''}>${c[1]}</label>`).join('');$('columnOptions').querySelectorAll('input').forEach(i=>i.onchange=()=>{i.checked?visible.add(i.dataset.col):visible.delete(i.dataset.col);if(visible.size<4){visible.add(i.dataset.col);i.checked=true}localStorage.setItem('sh_columns',JSON.stringify([...visible]));render()})}
document.addEventListener('click',e=>{const b=e.target.closest('[data-id]');if(b)openDetail(b.dataset.id)});$('closeDetail').onclick=()=>$('detailDialog').close();$('columnsBtn').onclick=openColumns;$('closeColumns').onclick=closeColumns;$('panelBackdrop').onclick=closeColumns;$('defaultColumns').onclick=()=>{visible=new Set(defaultKeys);localStorage.setItem('sh_columns',JSON.stringify([...visible]));renderColumnOptions();render()};$('allColumns').onclick=()=>{visible=new Set(columns.map(c=>c[0]));localStorage.setItem('sh_columns',JSON.stringify([...visible]));renderColumnOptions();render()};$('prevPage').onclick=()=>{if(page>1){page--;render()}};$('nextPage').onclick=()=>{page++;render()};$('refreshBtn').onclick=load;$('soundBtn').onclick=()=>{sound=!sound;$('soundBtn').textContent=sound?'🔔 هشدار روشن':'🔕 هشدار خاموش'};['search','hunt','decision'].forEach(id=>$(id).addEventListener(id==='search'?'input':'change',()=>{page=1;render()}));$('pageSize').onchange=()=>{page=1;render()};renderColumnOptions();if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js?v=4.0.5').catch(()=>{});load();setInterval(load,15000);
})();