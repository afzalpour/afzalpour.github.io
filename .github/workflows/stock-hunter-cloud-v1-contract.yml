name: Stock Hunter Cloud v1 Contract

on:
  pull_request:
    paths:
      - 'stock-hunter-v4/cloud-v1/**'
      - 'stock-hunter-v4/CLOUD_PRODUCTION_ARCHITECTURE_CANONICAL.md'
      - '.github/workflows/stock-hunter-cloud-v1-contract.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Frozen 4.1.6 asset guard
        shell: bash
        run: |
          set -euo pipefail
          if git diff --name-only origin/main...HEAD -- \
            stock-hunter-v4/app-hunt-v416.js \
            stock-hunter-v4/app-session-v413.js | grep -q .; then
            echo "Frozen Hunt/session assets changed. Cloud architecture PR must not modify them." >&2
            exit 1
          fi

      - name: Frozen runtime blob identity check
        shell: bash
        run: |
          set -euo pipefail
          test "$(git hash-object stock-hunter-v4/app-hunt-v416.js)" = "a5eead927382b5ced1c840697e6aaf21d52d8391"
          test "$(git hash-object stock-hunter-v4/app-session-v413.js)" = "84eb8564aecd9681c014a0978f7bd0dbc8e39f01"
          test "$(git hash-object stock-hunter-v4/hunt-parity-v416.js)" = "400fb64e841de6261d0e5e744c475e032f9c9dfe"
          test "$(git hash-object stock-hunter-v4/capture-security/stock-hunter-capture-v416/index.ts)" = "0c1edf26b1ff30c2829b507752b0c4ec5a7ca250"
          test "$(git hash-object stock-hunter-v4/legacy-security/stock-hunter-market-scan-v4/index.ts)" = "e2b2e9353af61ac3b479b52a0338ceac1a26fd0f"

      - name: Iran collector Python protocol self-test
        run: python3 stock-hunter-v4/cloud-v1/iran-collector/collector.py --self-test

      - name: Node protocol interoperability self-test
        run: node stock-hunter-v4/cloud-v1/tests/protocol-selftest.mjs

      - name: Foreign cloud must not call TSETMC directly
        shell: bash
        run: |
          set -euo pipefail
          if grep -RniE 'tsetmc|tsev2|MarketWatchInit|MarketWatchPlus|ClientTypeAll' \
            stock-hunter-v4/cloud-v1/cloudflare/src; then
            echo "Direct TSETMC reference found in foreign cloud runtime." >&2
            exit 1
          fi

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install pinned Cloudflare tooling
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: npm install --ignore-scripts --no-audit --no-fund

      - name: Generated frozen runtime drift check
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: npm run check:frozen

      - name: Exact browser to cloud Frozen Hunt parity
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: npm run parity

      - name: Generated live signal core drift check
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: npm run check:signal

      - name: Base live feature builder parity
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: npm run test:features

      - name: Metadata-only WebSocket fan-out contract
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: npm run test:websocket

      - name: Bounded R2 archive policy contract
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: npm run test:archive

      - name: Atomic ingest ordering contract
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: npm run test:ingest-order

      - name: R2 daily raw-pack compactor contract
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: npm run test:compactor

      - name: Staging cloud client fail-closed contract
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: npm run test:client

      - name: Mobile cloud staging isolation contract
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: node ../tests/cloud-mobile-stage-v1.mjs

      - name: Production frontend must not load staging cloud client
        shell: bash
        run: |
          set -euo pipefail
          if grep -Fq 'cloud-market-client-v1' stock-hunter-v4/index.html; then
            echo "Production index.html must not load the staging cloud client before cutover gates pass." >&2
            exit 1
          fi

      - name: Cloudflare dry-run compile
        working-directory: stock-hunter-v4/cloud-v1/cloudflare
        run: npm run check
